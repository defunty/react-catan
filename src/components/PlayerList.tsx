import React, { useState, useEffect }from 'react'

/*
type Users = {
  [key: string]: {[name: string]: string}
}
type Props = {
  users: Users,
  yourName: string
}

const PlayerList: React.FC = ({ children, users, yourName }) => {
  return (
    <ul className="PlayerList">
      { users.map(user => <li key={user.name} className={user.name === yourName && 'you' }>{user.name}</li>) }
    </ul>
  )
}
*/
const PlayerList = () => {
  return(<div></div>)
}

export default PlayerList;
